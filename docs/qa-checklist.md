# 갈피 수동 QA 체크리스트

## 패널
- [ ] ⌥Space로 열면 즉시 입력창에 커서가 있다 (클릭 불필요)
- [ ] Esc/⌥Space로 닫으면 직전 앱으로 포커스가 돌아간다
- [ ] 메뉴바 아이콘 좌클릭 토글 / 우클릭 메뉴가 동작한다
- [ ] 풀스크린 앱(예: 전체화면 Safari) 위에서도 패널이 뜬다
- [ ] 다른 앱을 클릭해도 패널이 닫히지 않고 떠 있다
- [ ] 패널이 떠 있어도 뒤 앱의 포커스를 뺏지 않는다

## 한글 IME (핵심 차별화 — 전부 통과 필수)
- [ ] 조합 중 Enter — 마지막 글자가 잘리지 않는다 ("갈피" 치고 바로 Enter)
- [ ] 조합 중 ⌘F — 글자가 유실되지 않는다
- [ ] 인라인 수정에서도 위 두 항목 통과
- [ ] 자소 분리("한글"→"ㅎㅏㄴ글")가 어디에도 없다
- [ ] 구름 입력기 등 서드파티 입력기에서도 기본 동작 확인

## 검색
- [ ] 초성(ㅅㅇㄷ), 부분(사이ㄷ 조합 중), 영문 대소문자 무시 모두 동작
- [ ] 하이라이트 위치가 정확하다 (겹받침 포함 단어로 확인)
- [ ] 태그 칩 클릭 필터 + 검색어 AND 결합
- [ ] Esc 1번=검색 종료, 2번=패널 닫기

## 샌드박스 (App Store 빌드 필수)
- [ ] `codesign -d --entitlements -` 출력에 app-sandbox = true
- [ ] 데이터가 ~/Library/Containers/kr.hurdlers.Galpi/ 안에 생성된다
- [ ] ⌥Space 전역 단축키가 손쉬운 접근 권한 없이 동작한다
- [ ] Esc로 닫으면 직전 앱(Safari 등)으로 포커스가 복귀한다
- [ ] 전체 내보내기가 NSSavePanel로 실제 파일을 쓴다
- [ ] 로그인 시 자동 시작 토글이 켜지고 꺼진다

## 데이터
- [ ] 재시작 후 데이터 유지
- [ ] Backups/에 오늘자 스냅샷, 8일 지나면 7개 유지 (날짜 조작 테스트는 스킵 가능 — 단위 테스트가 커버)
- [ ] 전체 내보내기 .md가 올바르다
- [ ] 1,000자 메모, 이모지 메모, 링크 텍스트 표시 정상

## 비주얼·모션
- [ ] 라이트/다크 전환 시 한지/먹 테마가 각각 의도대로
- [ ] MaruBuri↔Pretendard 전환 반영
- [ ] '동작 줄이기' 설정 시 모든 애니메이션 제거
- [ ] 시스템 폰트 크기·해상도(레티나/비레티나)에서 흐릿함 없음

## 이월된 확인 항목 (Task 12·13에서)
- [ ] 수정 중 ⌘Z — 삭제 복구가 아니라 입력 텍스트 undo로 동작 (삭제해 둔 메모가 있는 상태에서 다른 메모 수정 중 ⌘Z)
- [ ] 더블클릭이 텍스트 단어 선택과 충돌하지 않고 수정 모드로 진입
- [ ] ⌘F로 검색 진입 시 검색 필드에 즉시 포커스
- [ ] 검색 중 패널을 단축키로 숨겼다 다시 열면 검색 상태가 남는지 (남으면 기록만 — 결함 아님, 관찰)
- [ ] 설정에서 패널 좌/우·폭 변경이 다음 열기부터 반영
- [ ] 내보내기 실패 상황 대신 정상 내보내기 1회 (md 파일 열어 확인)
- [ ] ⌥Space가 다른 앱 단축키와 충돌하는지 (충돌 시 설정에서 변경 가능한지)

## v1.1 항목 (폴더·앱 크롬)
- [ ] 헤더: "갈피" 워드마크(굵게)·필터명 표시, 아이콘 3종(돋보기→검색, 서랍→드로어, 기어→설정) 동작
- [ ] 플로팅 패널: 상하·바깥 12pt 여백 + 라운드 + 그림자가 과하지 않게 한 겹
- [ ] 드로어: 서랍 아이콘으로 열기, 스크림 탭/Esc로 닫기, 전체·미분류·폴더별 메모 수 정확
- [ ] 폴더 생성(한글 이름, 조합 중 Enter 포함)·이름 변경·삭제(확인 알럿, 메모 미분류 이동)
- [ ] 폴더 선택 → 필터 적용 + 헤더 필터명 갱신; 태그 섹션 클릭 → 태그 필터
- [ ] 폴더 필터 중 ⌘F 검색 → 그 폴더 안에서만 검색
- [ ] 카드 호버: 아이콘 3종(배경 캡슐 위) 페이드인, 수정 모드 중 숨김
- [ ] 폴더로 이동(호버·우클릭 양쪽): 이동 후 현재 필터에서 사라짐, 드로어 카운트 갱신
- [ ] 빈 상태: 메모 0개일 때 안내 문구
- [ ] 활성 폴더 삭제 → "전체"로 복귀

## v1.2 항목 (체크리스트·칩 바·앱 크롬 손질)
- [ ] 헤더 기어 클릭 → 설정 창이 실제로 열림 (v1.1 버그 수정 확인 — 이전엔 무반응)
- [ ] 설정 "화면 모드": 시스템/밝게/어둡게 전환이 패널·설정 창에 즉시 반영, 재시작 후 유지
- [ ] 폴더 칩 바: 폴더 0개면 안 보임, 1개 이상이면 전체→폴더들→미분류 순서로 표시
- [ ] 칩 클릭 → 즉시 필터 전환 + 헤더 필터명 갱신, 선택 칩 쪽빛 배경, 폴더 많으면 가로 스크롤
- [ ] 컴포저: 입력창이 배경·테두리로 구분되어 보임, 비었을 때 "새 메모…" 플레이스홀더가 커서 위치와 정렬
- [ ] 컴포저 체크리스트 버튼 → 현재 줄 앞에 "[] " 삽입 (커서가 줄 중간이어도 줄 시작에)
- [ ] "[] 할 일" 입력·전송 → 카드에 체크박스로 렌더, 클릭 토글(취소선+흐림), "n/m" 진행 표시
- [ ] 여러 항목 + 일반 줄 혼합 메모에서 올바른 항목이 토글됨, `- [ ]` 접두도 인식
- [ ] 체크 토글 후 수정 모드 진입 → 원문에 [x]로 반영되어 있음, 내보내기 md에서 GitHub 체크리스트로 보임
- [ ] 수정한 메모: 푸터에 "· 수정 HH:mm" 병기 (다른 날 수정이면 "M월 d일 HH:mm"), 새 메모엔 없음
- [ ] 기존 v1·v1.1 항목 회귀 없음 (특히 한글 조합 Enter·검색·⌘Z·드로어)
